<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Medicines</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Add your CSS file if any -->
</head>
<body>
    <h1>All Medicines</h1>
    <div>
        <ul>
            <% for (let meds of allMeds) { %>
                <li>
                    <a href="#">
                        <strong><%= meds.Drug %></strong>
                    </a>
                    <br>
                    <img src="<%= meds.image %>" alt="<%= meds.Drug %> Image" style="max-width: 100px; max-height: 100px;">
                </li>
            <% } %>
        </ul>
    </div>
</body>
</html>
